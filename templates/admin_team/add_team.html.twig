{% extends 'base.html.twig' %}

{% block nav %}
    {% include 'admin_home/admin_nav.html.twig' %}
{% endblock %}


{% block title %}Edition/Création Team{% endblock %}

{% block body %}
<div class="container text-light mt-5">
   <h2 class="mb-5">
    {% if update_mode %}   
            Modification d'une team
    {% else %}
            Création d'une team
    {% endif %}
   </h2>

    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    
    {{ form_row(form.movie) }}
    {{ form_row(form.person) }}
    {{ form_row(form.job) }}

    <button type="submit" class="btn btn-warning">
        {% if update_mode %}
            Modifier
        {% else %}
            Ajouter
        {% endif %}
    </button>

    {{ form_end(form) }}
</div>
{% endblock %}